<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for ALL network access (the ~2.6GB model download). Flutter's
         template only injects INTERNET into the debug/profile manifests, so a
         signed RELEASE build has NO network unless it is declared here — this is
         why the release APK's model download failed at 0% on device. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Roadmap SLICE 1 (on-device gemma-4-E2B via flutter_gemma): the large
         model download runs as a foreground data-sync service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <!-- Android 13+ (Pixel): background_downloader's foreground service posts a
         progress notification; without runtime POST_NOTIFICATIONS the download
         task fails at 0%. Declared here + requested at runtime before download. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- WhatsApp-style press-and-hold voice notes (record package): the mic is
         needed to record; the camera is used by image_picker's "Cámara" attach
         option (gallery needs no permission). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Self-hosted OTA app update: the app downloads a newer signed APK from
         the paired engine and hands it to the system package installer
         (open_filex -> ACTION_VIEW on a FileProvider content:// URI). Without
         this the installer intent is blocked. The user still taps "Instalar"
         on the system dialog (unavoidable for a sideloaded app). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- Scheduled local notifications (flutter_local_notifications
         zonedSchedule): the "Boletín automático" daily trigger (and local
         reminders) fire at the chosen hour via AlarmManager.
           * SCHEDULE_EXACT_ALARM: exact alarms on Android 12+ (user-revocable
             special access; the code falls back to an inexact alarm when
             denied, e.g. Android 14+ defaults).
           * RECEIVE_BOOT_COMPLETED + the boot receiver: re-register pending
             scheduled notifications after a reboot (otherwise a restart
             silently drops the alarm). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Optional biometric app lock (fingerprint / face): local_auth's Android
         BiometricPrompt needs USE_BIOMETRIC (API 28+). With
         AuthenticationOptions(biometricOnly = false) the OS automatically
         offers the device credential (PIN/pattern/password) as a fallback, so
         no separate legacy USE_FINGERPRINT is required. Opt-in, default OFF. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <application
        android:label="LifeOS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Roadmap SLICE 1: OpenCL / vendor GPU libs used by flutter_gemma's
             GPU backend. All optional (required="false") so devices without
             them still install the app and can fall back to CPU. -->
        <uses-native-library android:name="libOpenCL.so" android:required="false"/>
        <uses-native-library android:name="libvndksupport.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL-car.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- DEVICE ASSISTANT (Etapa 1): handling ACTION_ASSIST makes LifeOS
                 selectable in Ajustes → Apps predeterminadas → Asistente
                 digital, so long-pressing power / the assistant gesture opens
                 Axi's chat with the mic armed. The assist-role button/gesture
                 only needs this activity filter (a full VoiceInteractionService
                 is system-app territory and NOT required for the role). The
                 activity is singleTask, so a warm launch arrives via
                 onNewIntent and a cold launch via the onCreate intent — both
                 are funneled to Flutter through the lifeos/assistant channel
                 in MainActivity. -->
            <intent-filter>
                <action android:name="android.intent.action.ASSIST"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- AXI KEYBOARD (IME): system-wide on-device dictation. The service
             binds as an input method (BIND_INPUT_METHOD is a system-signature
             permission that protects the binding — only the OS can bind).
             STT runs fully on-device (sherpa-onnx + the Whisper model the app
             already downloads); audio/text never leave the phone. An IME may
             use the mic while it is the active input method (RECORD_AUDIO is
             already declared above and granted through the LifeOS app). -->
        <service
            android:name=".ime.AxiImeService"
            android:exported="true"
            android:label="@string/axi_ime_label"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod"/>
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/axi_ime_method"/>
        </service>
        <!-- flutter_local_notifications scheduled-notification receivers
             (plugin-documented setup; the plugin's own manifest does NOT
             declare them): the first delivers the AlarmManager broadcast that
             posts the scheduled notification, the second re-registers pending
             schedules after reboot/app-update. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- "Axi habla" (flutter_tts): on Android 11+ package visibility is
             restricted, so the app cannot see the system TTS engine (and
             isLanguageAvailable / setLanguage for Spanish silently fails)
             unless the TTS service intent is declared here. -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
    </queries>
</manifest>
