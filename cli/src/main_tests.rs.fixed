14:            Commands::Init(_) =>Some(14:            Commands::Init(_) )) (), // Pass
23:            Commands::Init(args) =>Some(23:            Commands::Init(args) )) assert!(args.force),
32:            Commands::Init(args) =>Some(32:            Commands::Init(args) )) assert_eq!(args.profile.as_deref(), Some("developer")),
41:            Commands::Status(_) =>Some(41:            Commands::Status(_) )) (), // Pass
50:            Commands::Status(args) =>Some(50:            Commands::Status(args) )) assert!(args.json),
59:            Commands::Status(args) =>Some(59:            Commands::Status(args) )) assert!(args.detailed),
68:            Commands::Update(_) =>Some(68:            Commands::Update(_) )) (), // Pass
77:            Commands::Update(args) =>Some(77:            Commands::Update(args) )) assert!(args.dry_run),
86:            Commands::Update(args) =>Some(86:            Commands::Update(args) )) {
100:            Commands::Rollback =>Some(100:            Commands::Rollback )) (), // Pass
109:            Commands::Recover =>Some(109:            Commands::Recover )) (), // Pass
118:            Commands::Config(ConfigCommands::Show) =>Some(118:            Commands::Config(ConfigCommands::Show) )) (), // Pass
127:            Commands::Config(ConfigCommands::Get { key }) =>Some(127:            Commands::Config(ConfigCommands::Get { key }) )) {
138:            Commands::Config(ConfigCommands::Set { key, value }) =>Some(138:            Commands::Config(ConfigCommands::Set { key, value }) )) {
183:            Commands::FirstBoot(_) =>Some(183:            Commands::FirstBoot(_) )) (), // Pass
192:            Commands::FirstBoot(args) =>Some(192:            Commands::FirstBoot(args) )) assert!(args.gui),
201:            Commands::Focus =>Some(201:            Commands::Focus )) (),
210:            Commands::Meeting =>Some(210:            Commands::Meeting )) (),
297:            Commands::Intents(commands::intents::IntentsCommands::Apply { intent_id, approve }) =>Some(297:            Commands::Intents(commands::intents::IntentsCommands::Apply { intent_id, approve }) )) {
389:            Commands::Intents(commands::intents::IntentsCommands::Shield { input }) =>Some(389:            Commands::Intents(commands::intents::IntentsCommands::Shield { input }) )) {
400:            Commands::Intents(commands::intents::IntentsCommands::WorkspaceAwareness) =>Some(400:            Commands::Intents(commands::intents::IntentsCommands::WorkspaceAwareness) )) (),
553:            Commands::Id(commands::id::IdCommands::List { active }) =>Some(553:            Commands::Id(commands::id::IdCommands::List { active }) )) assert!(active),
589:            Commands::Workspace(commands::workspace::WorkspaceCommands::List { limit }) =>Some(589:            Commands::Workspace(commands::workspace::WorkspaceCommands::List { limit }) )) {
617:            Commands::Assistant(commands::assistant::AssistantCommands::InstallLauncher) =>Some(617:            Commands::Assistant(commands::assistant::AssistantCommands::InstallLauncher) )) (),
633:            Commands::Adapters(commands::adapters::AdaptersCommands::Image { path, prompt }) =>Some(633:            Commands::Adapters(commands::adapters::AdaptersCommands::Image { path, prompt }) )) {
652:            Commands::Voice(commands::voice::VoiceCommands::Transcribe { file, model }) =>Some(652:            Commands::Voice(commands::voice::VoiceCommands::Transcribe { file, model }) )) {
664:            Commands::Ai(commands::ai::AiCommands::Autotune { dry_run }) =>Some(664:            Commands::Ai(commands::ai::AiCommands::Autotune { dry_run }) )) assert!(dry_run),
673:            Commands::Ai(commands::ai::AiCommands::Profile { runtime, apply }) =>Some(673:            Commands::Ai(commands::ai::AiCommands::Profile { runtime, apply }) )) {
685:            Commands::Ai(commands::ai::AiCommands::Catalog { refresh }) =>Some(685:            Commands::Ai(commands::ai::AiCommands::Catalog { refresh }) )) assert!(refresh),
819:            Commands::Memory(commands::memory::MemoryCommands::Graph { limit, output }) =>Some(819:            Commands::Memory(commands::memory::MemoryCommands::Graph { limit, output }) )) {
853:            Commands::Sync(commands::sync::SyncCommands::Now { dry_run }) =>Some(853:            Commands::Sync(commands::sync::SyncCommands::Now { dry_run }) )) assert!(dry_run),
862:            Commands::Skills(commands::skills::SkillsCommands::Install { manifest }) =>Some(862:            Commands::Skills(commands::skills::SkillsCommands::Install { manifest }) )) {
910:            Commands::Skills(commands::skills::SkillsCommands::McpExport { output, trust }) =>Some(910:            Commands::Skills(commands::skills::SkillsCommands::McpExport { output, trust }) )) {
1018:            Commands::Browser(commands::browser::BrowserCommands::Run { policy, step }) =>Some(1018:            Commands::Browser(commands::browser::BrowserCommands::Run { policy, step }) )) {
1047:            Commands::Workflow(commands::workflow::WorkflowCommands::Validate { path }) =>Some(1047:            Commands::Workflow(commands::workflow::WorkflowCommands::Validate { path }) )) {
