# Allow lifeos user to manage llama-server GPU layers without password.
# Used by Game Guard to offload LLM from VRAM to RAM when gaming.
lifeos ALL=(root) NOPASSWD: /usr/local/bin/lifeos-llama-gpu-layers.sh
