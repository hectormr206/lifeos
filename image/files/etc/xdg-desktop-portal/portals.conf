# LifeOS: pin COSMIC as the preferred portal backend.
# Prevents D-Bus timeouts when gtk/wlr backends respond before cosmic.
# lifeos backend provides Camera, Microphone, RemoteDesktop, Secret via lifeosd.
[preferred]
default=cosmic;lifeos;gtk
