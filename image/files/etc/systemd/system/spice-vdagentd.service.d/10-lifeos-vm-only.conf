# LifeOS — Service Minimization (CIS Benchmark)
# spice-vdagent only needed inside VMs (clipboard/resize).
# ConditionVirtualization ensures it never starts on bare metal.
[Unit]
ConditionVirtualization=vm
