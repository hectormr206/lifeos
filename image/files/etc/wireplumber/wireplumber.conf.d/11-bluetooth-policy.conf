# LifeOS workaround: disable BT autoswitch to prevent volume desync
# between COSMIC panel and PipeWire sink (WirePlumber 0.5.13 bug).
# Remove when WirePlumber 0.5.14+ lands in Fedora.
wireplumber.settings = {
  bluetooth.autoswitch-to-headset-profile = false
}
